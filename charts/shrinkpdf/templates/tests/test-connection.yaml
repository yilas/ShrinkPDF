apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shrinkpdf.fullname" . }}-test-connection"
  labels:
    {{- include "shrinkpdf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shrinkpdf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
