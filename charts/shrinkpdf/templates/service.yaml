apiVersion: v1
kind: Service
metadata:
  name: {{ include "shrinkpdf.fullname" . }}
  labels:
    {{- include "shrinkpdf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "shrinkpdf.selectorLabels" . | nindent 4 }}
